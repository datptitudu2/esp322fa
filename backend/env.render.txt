# Environment Variables cho Render Deployment
# Copy nội dung này vào Render dashboard > Environment Variables > "Add from .env"

# Production environment
NODE_ENV=production

# Port (Render tự động set, nhưng có thể override)
PORT=10000

# Frontend URL (sẽ cập nhật sau khi deploy frontend)
# Tạm thời để * để cho phép tất cả origins (bao gồm ESP32 từ Wokwi)
FRONTEND_URL=*
CORS_ORIGIN=*

# Email configuration (Gmail)
EMAIL_USER=dattkdz@gmail.com
EMAIL_PASS=Itfrbzoxxykugfqt

